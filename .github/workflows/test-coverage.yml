name: test-coverage

on:
  pull_request:
#
permissions:
  contents: read
  pull-requests: write

jobs:
  coverage:
    uses: one-year-gap/infra/.github/workflows/test-coverage.yml@main
    secrets: inherit
    with:
      working-directory: .
      min-line-overall: 0.7
      min-branch-overall: 0.5
      min-line-changed: 0.8
      min-branch-changed: 0.6
