spring:
  config:
    import: "aws-secretsmanager:holliverse/api-server/runtime"
  cloud:
    aws:
      region:
        static: ap-northeast-2
      stack:
        auto: false
      secretsmanager:
        enabled: true
        fail-fast: true